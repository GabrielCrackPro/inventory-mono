// Re-export shared types for backward compatibility
export type { Room, RoomWithRelations, CreateRoomData, UpdateRoomData } from '@inventory/shared';

export { RoomType, PermissionLevel } from '@inventory/shared';

// Note: RoomAccess is in the access model, not room model
