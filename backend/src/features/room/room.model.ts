// Re-export shared types for backward compatibility
export type {
  Room,
  RoomWithRelations,
  CreateRoomData,
  UpdateRoomData,
  ShareRoomData,
  RoomDisplayData,
  RoomTypeInfo,
} from '@inventory/shared';

export { RoomType } from '@inventory/shared';
