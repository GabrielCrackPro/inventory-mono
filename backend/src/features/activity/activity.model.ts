// Re-export shared types for backward compatibility
export type {
  Activity,
  ActivityWithUser,
  UserActivity,
  CreateActivityData,
} from '@inventory/shared';

export { ActivityType } from '@inventory/shared';
