// Re-export shared types for backward compatibility
export type {
  Item,
  ItemWithRelations,
  CreateItemData,
  UpdateItemData,
  ItemSearchCriteria,
} from '@inventory/shared';

export { ItemCondition, ItemVisibility } from '@inventory/shared';
